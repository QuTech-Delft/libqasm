metadata:
  labels:
    test: libqasm
spec:
  containers:
  - name: libqasm
    image: qutech/libqasm:1.0.0-11-gf3eebd0
    command:
    - cat
    tty: true
    resources:
      requests:
        memory: "256Mi"
      limits:
        memory: "256Mi"
  - name: jnlp
    resources:
      requests:
        memory: "256Mi"
      limits:
        memory: "256Mi"
  imagePullSecrets:
    - name: regcred
