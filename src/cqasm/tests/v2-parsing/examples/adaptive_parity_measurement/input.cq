cQASM 2.0

qubit q;
var sel = false;

#...

while (true) {
    #...

    # toggle selector if q is measured as 1
    sel ^= measure(q);

    # perform gate 'a' or 'b', depending on 'sel'.
    if (sel) a(q) else b(q);

};
