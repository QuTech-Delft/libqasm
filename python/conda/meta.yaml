package:
  name: libqasm
  version: "{{ load_setup_py_data().version }}"

source:
  path: ../../.

build:
  number: 0
  script_env:
    - NPROCS

requirements:
  build:
    - {{ compiler('cxx') }}
    - setuptools
    - swig
    - cmake
    - flex # [not win]
    - bison # [not win]
    #- winflexbison # [win]
  host:
    - antlr4-tools
    - conan
    - python
    - plumbum
    - delocate # [osx]
  run:
    - python

test:
  imports:
    - libQasm
  source_files:
    - res
    - test
  requires:
    - pytest
  commands:
    - pytest test

about:
  home: https://github.com/QuTech-Delft/libqasm
  license: Apache v2.0
