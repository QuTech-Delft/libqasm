[build-system]
requires = [
    "setuptools",
    "swig"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
before-all = "uname -a"
build-verbosity = 3
skip = ["cp36-*", "cp37-*", "pp*"]
test-requires = "pytest"
test-command = "pytest {project}/test"
